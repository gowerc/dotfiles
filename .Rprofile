
options(
    "repos" = c(CRAN = "https://www.stats.bris.ac.uk/R"),
    setWidthOnResize = TRUE,
    HTTPUserAgent = sprintf(
        "R/%s R (%s)", 
		getRversion(), 
		paste(
			getRversion(), 
			R.version["platform"], 
			R.version["arch"], 
			R.version["os"]
		)
    )
)

